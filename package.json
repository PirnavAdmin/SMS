{
  "private": true,
  "scripts": {
    "start": "npm.cmd --prefix Frontend start",
    "build": "npm.cmd --prefix Frontend run build",
    "frontend": "npm.cmd --prefix Frontend start"
  },
  "dependencies": {
    "react-icons": "^5.6.0"
  }
}
